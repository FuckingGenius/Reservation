specVersion: "v1"
serviceId: "reservation"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "reservation-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
